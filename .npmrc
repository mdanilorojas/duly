# Hoist packages that need to be accessible across workspace packages.
# tailwindcss is in apps/docs/node_modules but @tailwindcss/postcss needs to
# resolve it when processing packages/tokens/dist/index.css at build time.
public-hoist-pattern[]=tailwindcss
